# UFO Galaxy 完整指南

**版本**: 1.0.0  
**最后更新**: 2026-02-11  
**状态**: ✅ 完成

---

## 📚 目录

1. [系统概述](#系统概述)
2. [快速开始](#快速开始)
3. [系统架构](#系统架构)
4. [节点管理](#节点管理)
5. [配置指南](#配置指南)
6. [API 文档](#api-文档)
7. [故障排查](#故障排查)
8. [常见问题](#常见问题)

---

## 系统概述

UFO Galaxy 是一个 **L4 级自主性智能系统**，集成了 108 个功能节点，支持多种 AI 模型和服务。

### 核心特性

- 🚀 **108 个功能节点** - 涵盖核心、开发、扩展、学术四大类
- 🔄 **自动依赖管理** - 115 个依赖关系完全验证
- 🛡️ **配置验证系统** - 启动前完整检查
- 📅 **每日自动完善** - 每天 09:00 自动运行
- 🌐 **多平台支持** - Windows、Linux、Mac
- 📊 **完整监控** - 系统状态实时监控

### 系统统计

| 指标 | 数值 |
|------|------|
| 总节点数 | 108 |
| 核心节点 | 15 |
| 开发工具 | 23 |
| 扩展功能 | 49 |
| 学术研究 | 21 |
| 依赖关系 | 115 |
| API 密钥 | 4+ |

---

## 快速开始

### 前置要求

- Python 3.8+
- Git
- 2GB 磁盘空间
- 网络连接

### Linux/Mac 用户

```bash
# 1. 进入项目目录
cd /home/ubuntu/delivery/ufo-galaxy-realization

# 2. 运行启动脚本
python main_fixed.py

# 3. 等待系统启动（约 30-60 秒）
```

### Windows 用户

```cmd
# 方法 1：使用批处理脚本
start_windows.bat

# 方法 2：使用 PowerShell
.\start_windows.ps1

# 方法 3：手动运行
python main_fixed.py
```

### 验证安装

```bash
# 运行配置验证
python config_validator.py

# 运行完整测试
python test_all_fixes.py
```

---

## 系统架构

### 目录结构

```
ufo-galaxy-realization/
├── nodes/                    # 108 个功能节点
│   ├── Node_00_StateMachine/
│   ├── Node_01_OneAPI/
│   ├── Node_02_Gemini/
│   └── ... (105 个其他节点)
├── core/                     # 核心模块
│   ├── unified_node_manager.py
│   ├── message_router.py
│   └── ...
├── launcher/                 # 启动器
│   ├── unified_launcher.py
│   ├── galaxy_launcher.py
│   └── ...
├── ui/                       # Web UI
│   ├── static/
│   ├── templates/
│   └── ...
├── data/                     # 数据存储
├── docs/                     # 文档
├── main_fixed.py             # 修复版启动脚本
├── config.yaml               # 系统配置
├── node_dependencies.json    # 节点依赖配置
└── ...
```

### 启动流程

```
┌─────────────────────────────────┐
│  1. 依赖检查                     │
│  - 验证 Python 版本             │
│  - 检查必需库                   │
└──────────────┬──────────────────┘
               │
┌──────────────▼──────────────────┐
│  2. 环境变量加载                 │
│  - 读取 .env 文件               │
│  - 验证 API 密钥                │
└──────────────┬──────────────────┘
               │
┌──────────────▼──────────────────┐
│  3. 节点配置加载                 │
│  - 读取 node_dependencies.json  │
│  - 验证节点定义                 │
└──────────────┬──────────────────┘
               │
┌──────────────▼──────────────────┐
│  4. 配置验证                     │
│  - 检查依赖关系                 │
│  - 验证节点分组                 │
│  - 检查端口冲突                 │
└──────────────┬──────────────────┘
               │
┌──────────────▼──────────────────┐
│  5. 节点初始化                   │
│  - 加载节点模块                 │
│  - 初始化节点实例               │
└──────────────┬──────────────────┘
               │
┌──────────────▼──────────────────┐
│  6. 系统启动                     │
│  - 启动 Web UI                  │
│  - 启动 API 服务                │
│  - 启动监控系统                 │
└─────────────────────────────────┘
```

---

## 节点管理

### 节点分类

#### 核心节点 (15 个)

关键的系统功能节点，必须正常运行。

```
Node_00_StateMachine     - 状态机
Node_01_OneAPI          - OneAPI 集成
Node_02_Gemini          - Google Gemini
Node_03_Grok            - xAI Grok
... (11 个其他核心节点)
```

#### 开发工具 (23 个)

支持开发和测试的工具节点。

```
Node_20_CodeGenerator   - 代码生成
Node_21_Debugger        - 调试工具
Node_22_Profiler        - 性能分析
... (20 个其他开发工具)
```

#### 扩展功能 (49 个)

增强系统功能的扩展节点。

```
Node_40_DataProcessor   - 数据处理
Node_41_ImageAnalysis   - 图像分析
Node_42_AudioProcess    - 音频处理
... (46 个其他扩展功能)
```

#### 学术研究 (21 个)

支持学术研究和高级分析的节点。

```
Node_80_MachineLearning - 机器学习
Node_81_NLP             - 自然语言处理
Node_82_ComputerVision  - 计算机视觉
... (18 个其他学术节点)
```

### 节点操作

#### 查看节点状态

```bash
python config_validator.py
```

#### 启动特定节点

```bash
# 在 main_fixed.py 中修改启动模式
# mode = "all"      # 启动所有节点
# mode = "core"     # 仅启动核心节点
# mode = "minimal"  # 最小化启动
```

#### 重启节点

```bash
# 系统会自动监控并重启失败的节点
# 最多重启 3 次（可在 config.yaml 中配置）
```

---

## 配置指南

### config.yaml 配置

系统配置文件 `config.yaml` 包含所有系统设置。

#### 系统基本配置

```yaml
system:
  name: "UFO Galaxy"
  version: "1.0.0"
  mode: "production"  # development, staging, production
  logging:
    level: "INFO"     # DEBUG, INFO, WARNING, ERROR, CRITICAL
```

#### 节点配置

```yaml
nodes:
  loading:
    parallel: true        # 并行加载
    max_workers: 4        # 最大并行数
    timeout: 30           # 加载超时
    validate: true        # 验证配置
  startup:
    mode: "all"           # all, core, minimal
    order: "priority"     # 启动顺序
    timeout: 60           # 启动超时
    continue_on_error: false  # 失败是否继续
```

#### Web UI 配置

```yaml
ui:
  enabled: true
  host: "0.0.0.0"
  port: 8080
  ssl: false
```

#### 数据库配置

```yaml
database:
  type: "sqlite"
  path: "data/galaxy.db"
```

### 环境变量

创建 `.env` 文件配置 API 密钥：

```bash
# OpenAI
OPENAI_API_KEY=sk-...

# Google Gemini
GEMINI_API_KEY=...

# xAI Grok
XAI_API_KEY=...

# OpenRouter
OPENROUTER_API_KEY=...
```

---

## API 文档

### REST API

所有 API 都通过 `/api/v1/` 前缀访问。

#### 获取系统状态

```bash
GET /api/v1/system/status
```

**响应**：
```json
{
  "status": "running",
  "nodes_total": 108,
  "nodes_active": 108,
  "uptime": 3600,
  "version": "1.0.0"
}
```

#### 获取节点列表

```bash
GET /api/v1/nodes
```

**响应**：
```json
{
  "nodes": [
    {
      "id": "00",
      "name": "Node_00_StateMachine",
      "group": "core",
      "status": "running",
      "port": 5000
    },
    ...
  ]
}
```

#### 获取节点详情

```bash
GET /api/v1/nodes/{node_id}
```

#### 执行节点命令

```bash
POST /api/v1/nodes/{node_id}/execute
Content-Type: application/json

{
  "command": "...",
  "params": {...}
}
```

### WebSocket API

实时通信通过 WebSocket 实现。

```javascript
// 连接到 WebSocket
const ws = new WebSocket('ws://localhost:8080/api/v1/ws');

// 发送消息
ws.send(JSON.stringify({
  type: 'command',
  node_id: '00',
  command: 'execute',
  params: {}
}));

// 接收消息
ws.onmessage = (event) => {
  const message = JSON.parse(event.data);
  console.log(message);
};
```

---

## 故障排查

### 常见问题

#### 1. 系统启动失败

**症状**：运行 `python main_fixed.py` 后立即退出

**解决方案**：
```bash
# 1. 运行配置验证
python config_validator.py

# 2. 运行自动修复
python auto_fix.py

# 3. 检查日志
tail -f logs/system.log
```

#### 2. 节点无法加载

**症状**：某些节点显示 "failed" 状态

**解决方案**：
```bash
# 1. 检查节点配置
python config_validator.py

# 2. 检查节点目录
ls -la nodes/Node_XX_*/

# 3. 检查依赖关系
python -c "import json; print(json.load(open('node_dependencies.json'))['nodes']['Node_XX'])"
```

#### 3. 端口被占用

**症状**："Address already in use" 错误

**解决方案**：
```bash
# 1. 查找占用端口的进程
lsof -i :5000  # 或其他端口号

# 2. 杀死进程
kill -9 <PID>

# 3. 修改 config.yaml 中的端口
```

#### 4. API 密钥无效

**症状**：API 调用返回 401 或 403 错误

**解决方案**：
```bash
# 1. 检查 .env 文件
cat .env

# 2. 验证 API 密钥格式
# 3. 重新启动系统
```

### 日志位置

```
logs/
├── system.log      # 系统日志
├── api.log         # API 日志
├── nodes.log       # 节点日志
└── error.log       # 错误日志
```

### 调试模式

在 `config.yaml` 中启用调试模式：

```yaml
development:
  debug: true
  hot_reload: true
  profiling: true
```

---

## 常见问题

### Q: 系统需要多少资源？

**A**: 
- CPU: 2+ 核心
- 内存: 4GB+
- 磁盘: 2GB+
- 网络: 10Mbps+

### Q: 支持哪些操作系统？

**A**: 
- Windows 10/11
- Linux (Ubuntu 20.04+)
- macOS 10.15+

### Q: 如何添加新节点？

**A**: 
1. 在 `nodes/` 目录创建新目录 `Node_XX_YourNodeName/`
2. 创建 `main.py` 和 `config.json`
3. 更新 `node_dependencies.json`
4. 运行 `python config_validator.py` 验证
5. 重启系统

### Q: 如何修改系统配置？

**A**: 
1. 编辑 `config.yaml`
2. 运行 `python config_validator.py` 验证
3. 重启系统

### Q: 如何查看系统日志？

**A**: 
```bash
# 实时查看系统日志
tail -f logs/system.log

# 查看最后 100 行
tail -100 logs/system.log

# 搜索特定错误
grep "ERROR" logs/system.log
```

### Q: 系统会自动备份数据吗？

**A**: 是的。系统每天自动备份数据到 `data/backups/` 目录。

### Q: 如何禁用自动完善任务？

**A**: 
1. 修改定时任务配置
2. 或在 `config.yaml` 中禁用：
```yaml
updates:
  enabled: false
```

---

## 支持和反馈

- 📧 **邮件**: support@ufo-galaxy.com
- 💬 **问题追踪**: GitHub Issues
- 📚 **文档**: /docs 目录
- 🐛 **报告 Bug**: GitHub Issues

---

## 许可证

MIT License - 详见 LICENSE 文件

---

**最后更新**: 2026-02-11  
**维护者**: UFO Galaxy Team
